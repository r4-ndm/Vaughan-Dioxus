[package]
name = "vaughan-ipc-types"
version.workspace = true
edition.workspace = true
description = "Shared IPC message types for wallet <-> dApp browser"

[dependencies]
serde = { version = "1", features = ["derive"] }
